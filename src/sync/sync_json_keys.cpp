// Translation unit anchor for sync_json_keys.h. The header defines all
// constants as inline constexpr (C++17), so no out-of-line storage is
// strictly required. This file exists so the {h,cpp} pair shape requested
// by the plan is honored and so the CMake source list has a stable entry.

#include "sync/sync_json_keys.h"
